# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# JSON-LD component embeds a <script> tag with a templated payload that
# Prettier mis-parses as JSON.
src/lib/components/atoms/JsonLd.svelte

# OpenSpec template folders (regenerated by `openspec update` / `openspec init`)
.agents
.claude
.codex
.github
.serena
.svelte-kit
