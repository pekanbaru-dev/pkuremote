import Root, { type PaginationProps, paginationItemVariants } from "./pagination.svelte";

export {
	Root,
	type PaginationProps as Props,
	Root as Pagination,
	paginationItemVariants,
	type PaginationProps
};
