## 1. Material Symbols + design tokens in app.html and layout.css

- [ ] 1.1 Open `src/app.html` and add a second Google Fonts `<link>` for Material Symbols Outlined: `https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap`. Keep the existing Hanken Grotesk + Manrope link.
- [ ] 1.2 In `src/routes/layout.css`, add the `.hero-clip` utility class with `clip-path: polygon(0 0, 100% 0, 100% 90%, 0% 100%)`.
- [ ] 1.3 In `src/routes/layout.css`, add the `.talam-shadow` utility class with `box-shadow: 0px 4px 20px rgba(233, 196, 106, 0.15)`.
- [ ] 1.4 In `src/routes/layout.css`, add the `.talam-gradient` utility class with `background: linear-gradient(180deg, #FEFAE0 0%, #F5E386 100%)`.
- [ ] 1.5 In `src/routes/layout.css`, add the `--hero-pattern` CSS custom property holding the Stitch Pucuk Rebung batik motif as a data-URI SVG (stroke `#e9c46a`, stroke-widths 2/1.5/1, opacity 0.8/0.6/0.4/0.5, two circles at center and 40,70 / 60,70, 200×200 viewBox). The SVG SHALL be base64-encoded.
- [ ] 1.6 In `src/routes/layout.css`, add the `.hero-pattern` utility class with `background-image: var(--hero-pattern); background-repeat: repeat; background-size: 100px 100px`.
- [ ] 1.7 In `src/routes/layout.css`, add the new spacing tokens to `@theme`: `--spacing-margin-mobile: 16px`, `--spacing-margin-desktop: 80px`, `--spacing-gutter: 24px`, `--spacing-xs: 4px`, `--spacing-sm: 12px`, `--spacing-base: 8px`, `--spacing-md: 24px`, `--spacing-lg: 40px`, `--spacing-xl: 64px`. Add the radius token `--radius-card: 0.75rem` (12px) with a comment noting `--radius-pill: 9999px` is the canonical "pill" radius.
- [ ] 1.8 Run `pnpm check` and confirm `@theme` is parsed without errors.

## 2. Event type + dummy data

- [ ] 2.1 In `src/lib/features/events/types.ts`, add two optional fields to the `Event` type: `category?: string` and `categorySecondary?: string`. Both max 16 chars. Document the meaning in a JSDoc comment.
- [ ] 2.2 In `src/lib/features/events/services/dummy-events.ts`, add `category` and `categorySecondary` to all 8 events. Use values that match the existing event titles (e.g., "Workshop" + "Hands-on" for the Svelte 5 event, "Talk" + "Networking" for the Design Systems event, "Meetup" + "Coffee Chat" for the coffee morning, "Social" + "Book Club" for the reading night, "Workshop" + "Masterclass" for the typography event, "Workshop" + "IoT" for the ESP32 event, "Talk" + "Data" for the data engineering event, "Social" + "Wellness" for the mental health event).
- [ ] 2.3 Run `pnpm check` and confirm the type change passes.

## 3. EventCard rewrite (Stitch style)

- [ ] 3.1 Open `src/lib/features/events/components/event-card.svelte`. Replace the existing component with the Stitch style: `class="bg-surface-container-lowest rounded-xl talam-shadow border-b-2 border-primary-container overflow-hidden group"`.
- [ ] 3.2 Replace the banner container with a `class="h-48 overflow-hidden"` div containing the `<img>` with `class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"`. The banner SHALL be 192px tall (h-48) and fill the card top.
- [ ] 3.3 Below the banner, render a `class="p-md"` div containing (in order): (a) a `class="flex gap-sm mb-sm"` row with two pill badges — `<span class="px-3 py-1 bg-primary/10 text-primary rounded-full text-label-md">{category}</span>` and `<span class="px-3 py-1 bg-secondary/10 text-secondary rounded-full text-label-md">{categorySecondary}</span>`. Each pill SHALL be hidden when its data field is undefined.
- [ ] 3.4 Render the title as `<h3 class="font-headline-md text-headline-md mb-xs">{event.title}</h3>`.
- [ ] 3.5 Render the excerpt as `<p class="text-on-surface-variant mb-md line-clamp-2">{event.excerpt}</p>`.
- [ ] 3.6 Render a footer row `class="flex items-center justify-between mt-auto"` containing (a) a left-aligned date with a Material Symbols calendar icon: `<span class="material-symbols-outlined text-sm mr-1">calendar_today</span> {formatted date}` and (b) a right-aligned CTA link with category-mapped label: `<a class="text-primary font-bold hover:translate-x-1 transition-transform">{ctaLabel}</a>`. The `ctaLabel` is selected by a small mapping: `workshop` → "Book Now", `meetup`/`talk` → "RSVP", `social`/`other` → "Register".
- [ ] 3.7 The whole card SHALL be a single `<a>` linking to `/events/{event.slug}`, with the accessible name being the visible title.
- [ ] 3.8 Run `pnpm check` and confirm the component compiles.

## 4. Homepage rewrite (Stitch composition)

- [ ] 4.1 Open `src/routes/+page.svelte` and remove the existing hero, "Event Akan Datang" section, "Event Sebelumnya" section, and the 2-column footer. Keep the `<svelte:head>` block (title, description, canonical, OG, Twitter Card) and the imports for `PUBLIC_SITE_URL`, `getUpcomingEvents`, `EmptyState`.
- [ ] 4.2 Replace the header with the Stitch version: a sticky `bg-surface` header with a 1px bottom border, an `h-20` nav with `max-w-[1280px] mx-auto` and `px-margin-desktop`, containing (a) the wordmark "PKUBersua" in `font-headline-md text-headline-md font-bold text-primary` (left), (b) a hidden-on-mobile nav with links to `#upcoming`, `#partners`, `#contact` styled `font-label-lg text-label-lg` (center), (c) a right group with a search bar `class="hidden lg:flex items-center bg-surface-container-low rounded-full px-4 py-2 border border-outline-variant"` containing a `<span class="material-symbols-outlined text-on-surface-variant mr-2">search</span>` icon and an `<input class="bg-transparent border-none focus:ring-0 text-label-md w-40" placeholder="Search community...">` input, and (d) a `<button class="bg-primary text-on-primary px-lg py-sm rounded-full font-label-lg hover:opacity-90 transition-all active:scale-95">Login/Register</button>` pill button.
- [ ] 4.3 Add a `<script>` import for the new `EventCard` and `EventList` exports from `$lib/features/events` (or import `EventCard` directly if the new style doesn't use `EventList`).
- [ ] 4.4 Replace the hero with the Stitch 870px hero: `<section class="relative w-full h-[870px] bg-surface overflow-hidden flex items-center hero-clip">`. Inside, in order: (a) `<div class="absolute inset-0 z-0 opacity-[0.07] pointer-events-none hero-pattern"></div>`, (b) `<div class="absolute inset-0 z-0"><div class="w-full h-full bg-cover bg-center opacity-40" style="background-image: url('https://lh3.googleusercontent.com/aida/...mosque.jpg')"></div><div class="absolute inset-0 bg-gradient-to-r from-surface via-surface/80 to-transparent"></div></div>`, (c) `<div class="relative z-10 px-margin-desktop max-w-[1280px] mx-auto w-full"><div class="max-w-2xl"><span class="inline-block px-4 py-1 bg-secondary-container text-on-secondary-container rounded-full font-label-lg mb-md">Pekanbaru Heritage &amp; Culture</span><h1 class="font-headline-xl text-headline-xl text-primary mb-md leading-tight">Celebrating the Heart of <br/><span class="text-secondary">Riau's Local Heritage</span></h1><p class="font-body-lg text-body-lg text-on-surface-variant mb-lg">Inspired by the layered sweetness of Talam Durian, our community brings together the rich traditions and modern aspirations of Pekanbaru residents.</p><div class="flex gap-md"><button class="bg-primary-container text-on-primary-container px-xl py-md rounded-lg font-headline-md shadow-sm hover:shadow-md transition-all active:scale-95">Explore Events</button><button class="border-2 border-primary text-primary px-xl py-md rounded-lg font-headline-md hover:bg-primary/5 transition-all active:scale-95">Learn History</button></div></div></div>`. Use the actual `lh3.googleusercontent.com` URL for the bg image documented in `design.md`.
- [ ] 4.5 Replace the events section with the Stitch "Upcoming Community Gatherings" 3-column grid: `<section class="py-xl px-margin-desktop max-w-[1280px] mx-auto" id="upcoming"><div class="flex justify-between items-end mb-xl"><div><h2 class="font-headline-lg text-headline-lg text-primary">Upcoming Community Gatherings</h2><p class="text-on-surface-variant mt-2">Join us for cultural workshops, food festivals, and local meetups.</p></div><a class="text-primary font-label-lg flex items-center gap-xs hover:underline" href="#">View All Events <span class="material-symbols-outlined">arrow_forward</span></a></div><div class="grid grid-cols-1 md:grid-cols-3 gap-gutter">{event cards}</div></section>`. The events SHALL be sliced to the first 3 upcoming (`getUpcomingEvents().slice(0, 3)`). The section SHALL be omitted entirely when the array is empty.
- [ ] 4.6 Add the "Empower Your Business" CTA section: `<section class="py-xl px-margin-desktop"><div class="max-w-[1280px] mx-auto bg-primary-container rounded-3xl p-xl flex flex-col md:flex-row items-center justify-between gap-lg relative overflow-hidden"><div class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full -mr-32 -mt-32"></div><div class="md:w-3/5 relative z-10"><h2 class="font-headline-xl text-headline-xl text-on-primary-container mb-md">Empower Your Business Through Community</h2><p class="font-body-lg text-body-lg text-on-primary-container/90 mb-xl">Partner with Pekanbaru Community to reach a highly engaged local audience. From event sponsorship to cultural collaborations, let's grow together.</p><div class="flex flex-wrap gap-md"><button class="bg-on-primary-container text-white px-xl py-md rounded-full font-headline-md hover:opacity-90 transition-all active:scale-95">Become a Partner</button><button class="bg-white/20 text-on-primary-container px-xl py-md rounded-full font-headline-md border border-on-primary-container/30 hover:bg-white/30 transition-all active:scale-95">Sponsorship Kit</button></div></div><div class="md:w-2/5 relative z-10"><div class="bg-white/40 backdrop-blur-md p-lg rounded-2xl border border-white/20 talam-shadow">{Community Impact card}</div></div></div></section>`. The Impact card SHALL contain a Material Symbols `trending_up` icon in a `bg-on-primary-container text-white rounded-lg p-2` square, the title "Community Impact", a "Growth metrics from last quarter" subtitle, and three labeled numbers (New Members: +1,240, Event Attendees: 5,800+, Partner Visibility: 150k Reach).
- [ ] 4.7 Add the "Trusted by Partners" section: `<section class="py-xl px-margin-desktop bg-surface" id="partners"><div class="max-w-[1280px] mx-auto"><div class="mb-xl"><h2 class="font-headline-lg text-headline-lg text-primary mb-2">Trusted by Local &amp; Global Partners</h2><p class="text-on-surface-variant">Collaborating with industry leaders to preserve and promote our heritage.</p></div><div class="grid grid-cols-2 sm:grid-cols-4 gap-lg">{4 logo tiles}</div></div></section>`. Each logo tile SHALL be `<div class="flex items-center justify-center p-md grayscale opacity-40 hover:grayscale-0 hover:opacity-100 transition-all duration-300 cursor-pointer"><img alt="Partner N logo" class="h-12 w-auto object-contain" src="/partners/logo-N.svg"></div>`.
- [ ] 4.8 Replace the footer with the Stitch 4-column footer: `<footer class="bg-surface-container-highest border-t border-secondary-fixed py-xl"><div class="max-w-[1280px] mx-auto px-margin-desktop grid grid-cols-1 md:grid-cols-4 gap-gutter">{4 columns}</div><div class="max-w-[1280px] mx-auto px-margin-desktop mt-xl pt-lg border-t border-outline-variant/30 text-center text-label-md text-on-surface-variant">© 2026 PKUBersua</div></footer>`. The 4 columns SHALL be: (1) brand column (wordmark + tagline + 3 social-icon links using Material Symbols `public`, `alternate_email`, `share`), (2) "The Community" nav column with 4 links, (3) "Support & Partnership" nav column with 4 links, (4) "Stay Connected" column with paragraph + email input + Join button.
- [ ] 4.9 Update the `<svelte:head>` `<title>` to keep the placeholder `[TAGLINE_PKUBERSUA_TBD]` pattern (the hero now has its own h1; the title remains the page-level meta).
- [ ] 4.10 Run `pnpm check` and confirm the homepage compiles.

## 5. Placeholder partner logos

- [ ] 5.1 Create `static/partners/` directory.
- [ ] 5.2 Create `static/partners/logo-1.svg` as a 200×60 monochrome SVG wordmark reading "Partner One" in a generic sans-serif. The fill SHALL be `currentColor` so the grayscale hover effect works.
- [ ] 5.3 Create `static/partners/logo-2.svg` as a 200×60 monochrome SVG wordmark reading "Partner Two".
- [ ] 5.4 Create `static/partners/logo-3.svg` as a 200×60 monochrome SVG wordmark reading "Partner Three".
- [ ] 5.5 Create `static/partners/logo-4.svg` as a 200×60 monochrome SVG wordmark reading "Partner Four".
- [ ] 5.6 Create `static/partners/README.md` explaining that the four placeholders are deliberately generic and SHOULD be replaced with real partner logos when the project has actual partners. Document the file naming convention (`logo-N.svg`), the expected dimensions (200×60), and the `currentColor` fill requirement.

## 6. Verification

- [ ] 6.1 Run `pnpm check` and confirm 0 errors and 0 warnings.
- [ ] 6.2 Run `pnpm lint` and confirm prettier and eslint pass. (The 1 pre-existing eslint error in `oauth-callback.test.ts` is acceptable.)
- [ ] 6.3 Run `pnpm test:unit -- --run` and confirm all 34 unit tests still pass.
- [ ] 6.4 Run `pnpm dev` and screenshot the homepage at 360px, 768px, and 1280px viewports. Confirm: the hero is 870px with the batik pattern visible, the bg image is visible at 40% opacity, the hero-clip slants the bottom edge, the dual CTAs sit on the left, the events grid is 1-col on mobile and 3-col on desktop, the Empower CTA renders the gold card with the Impact metric on the right, the Trusted by Partners renders 4 placeholders in grayscale, the footer is 4-col on desktop and 1-col on mobile.
- [ ] 6.5 Inspect the rendered HTML and confirm: every `<img>` has `loading="lazy"` and `decoding="async"`, the canonical link is present, the JSON-LD is unchanged from the previous change (events detail only).
- [ ] 6.6 Run the brand grep one more time: `git grep -in 'PKU Remote\|pku-remote\|pkuremote\|pekanbaru.dev' -- 'src' 'db' 'PRODUCT.md' 'DESIGN.md' 'AGENTS.md' 'openspec' 'static'`. Confirm zero matches.

## 7. OpenSpec archive

- [ ] 7.1 After all 6 sections are checked, run `openspec archive align-stitch-landing --yes` to move the change into `openspec/changes/archive/YYYY-MM-DD-align-stitch-landing/` and promote the new `landing-stitch` spec plus the modified `events` and `landing-page` deltas to canonical.
- [ ] 7.2 Verify the archive directory contains the four artifacts (`proposal.md`, `design.md`, `tasks.md`, `specs/`).
- [ ] 7.3 Verify `openspec list` no longer shows `align-stitch-landing` as an active change.

## 8. Operator follow-up (not in repo)

- [ ] 8.1 Replace the literal `[TAGLINE_PKUBERSUA_TBD]` in `src/routes/+page.svelte` with the real tagline once the brand team supplies copy.
- [ ] 8.2 Replace the four placeholder partner logos in `static/partners/` with real partner SVGs (200×60, `currentColor` fill) when the project has actual partners. Document the replacement in `static/partners/README.md`.
- [ ] 8.3 Confirm the Great Mosque of An-Nur background image licensing is acceptable for production. If not, replace the bg image URL with a self-hosted image (move to `static/images/hero-mosque.jpg` and update the URL in `+page.svelte`).
- [ ] 8.4 Wire the search input in the header to the events service (`getUpcomingEvents()` filtered by title) and the email signup in the footer to a newsletter endpoint. Both are decorative for this change.
