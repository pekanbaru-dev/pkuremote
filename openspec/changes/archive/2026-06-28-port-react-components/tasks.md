## 1. Phase 1 — Pilot (button, badge, empty-state)

- [x] 1.1 Create top-level barrel `src/lib/components/ui/index.ts` re-exporting the seven existing primitives (`Button`, `Card`, `Badge`, `Separator`, `Skeleton`, `AspectRatio`, `NavigationMenu`).
- [x] 1.2 Rebrand `src/lib/components/ui/button/button.svelte` TV variants to brand tokens: drop `bg-primary-foreground` text-only fallback, add `hover:bg-primary/90` on `default`, map `destructive` to existing destructive styling, collapse any non-canonical intents to brand accent.
- [x] 1.3 Rebrand `src/lib/components/ui/badge/` TV variants (when present) to brand tokens; align color usage with the One Voice Rule. (No-op: no badge source in `tmp/components/`, and the `src/lib/components/ui/badge/` folder is empty. A badge component will be created in a later phase if needed for landing-page integration.)
- [x] 1.4 Create `src/lib/components/ui/empty-state/empty-state.svelte` with TV variants (icon size, alignment, padding) and props (`title` required, `description` optional, `icon` snippet, `action` snippet, `class` optional).
- [x] 1.5 Create `src/lib/components/ui/empty-state/index.ts` re-exporting `EmptyState` and the `EmptyStateProps` type.
- [x] 1.6 Create `src/lib/components/ui/empty-state/empty-state.test.ts` smoke-render test using `vitest-browser-svelte`.
- [x] 1.7 Verify `pnpm check && pnpm lint && pnpm test:unit -- --run` is green; visually confirm landing page still renders at `http://localhost:5173`. (`pnpm check` and `pnpm test:unit -- --run` pass cleanly with the new files. `pnpm lint` reports 4 errors, all pre-existing in `tmp/components/*` and `src/lib/server/auth/oauth-callback.test.ts` — none from the new files. Visual browser confirmation is left to the user.)
- [x] 1.8 Delete `tmp/components/button/`, `tmp/components/badge/`, `tmp/components/empty-state/`.

## 2. Phase 2a — Primitive spikes and helpers

- [x] 2.1 Run a `bits-ui` parity spike for every primitive in phase 2: confirm which `bits-ui` primitive maps to each Radix import in the React source. Document gaps (autocomplete, custom dropdown menu items, menubar submenus) in `openspec/changes/port-react-components/design.md` Open Questions. (5 Radix imports found: dialog, dropdown, menubar, tabs, avatar — all 5 have direct `bits-ui` counterparts. No gaps. Autocomplete in React source is built bespoke with combobox patterns; bits-ui has `combobox` and `command` primitives to build it from.)
- [x] 2.2 Decide the `bits-ui` workarounds for the gaps: either build from lower-level primitives (Popover + custom list), pin a different version, or defer the component to phase 3. (No-op: no gaps. All 5 Radix imports have direct `bits-ui` counterparts.)
- [x] 2.3 Create `src/lib/components/ui/index.ts` barrel entries for every component added in phase 2 (placeholders for the names that will be added). (No-op: barrel exists from task 1.1; entries are added in-task as each component lands.)

## 3. Phase 2b — Form and choice primitives

- [x] 3.1 Convert `tmp/components/input/` → `src/lib/components/ui/input/`. Single file. TV variants for size and intent. Props mirror React source.
- [x] 3.2 Convert `tmp/components/textarea/` → `src/lib/components/ui/textarea/`. Single file. TV variants for size. Props mirror React source.
- [x] 3.3 Convert `tmp/components/file/` → `src/lib/components/ui/file/`. Single file. Drop-zone styling. Props mirror React source. (Note: converted to a plain `<input type="file">` rather than the React source's FilePond integration; FilePond is a heavy 2-dep library with no direct Svelte port. If FilePond is required, defer to a follow-up change.)
- [x] 3.4 Convert `tmp/components/radio/` → `src/lib/components/ui/radio/`. Single file. Build on `bits-ui` RadioGroup.Item. (Used a plain styled `<input type="radio">` since the React source's `Radio` is a single input rather than a group member; RadioGroup composes Radio via children.)
- [x] 3.5 Convert `tmp/components/radio-group/` → `src/lib/components/ui/radio-group/`. Single file. Wraps `bits-ui` RadioGroup.Root.
- [x] 3.6 Convert `tmp/components/autocomplete/` → `src/lib/components/ui/autocomplete/`. Compound or single file per spike result. Uses `bits-ui` Combobox if available, else Popover + custom list. (Built with `bits-ui` Combobox. Switched the option type from a generic `{value, label}` to support arbitrary `T`; consumers pass `options: { value: string; label: string }[]`. This is more idiomatic with `bits-ui`'s string-based item values.)
- [x] 3.7 Delete `tmp/components/{input,textarea,file,radio,radio-group,autocomplete}/`.

## 4. Phase 2c — Container primitives (compound)

- [x] 4.1 Convert `tmp/components/dialog/` → `src/lib/components/ui/dialog/`. Split into `dialog.svelte` (re-export group), `dialog-content.svelte`, `dialog-header.svelte`, `dialog-footer.svelte`, `dialog-title.svelte`, `dialog-description.svelte`, `dialog-overlay.svelte`, `dialog-close.svelte`, `dialog-portal.svelte` (if needed). Use `bits-ui` Dialog. Index groups subcomponents under a `Dialog` object. (Deviation: `<DialogPrimitive.Label>` from the React source's `DropdownMenu.Label` is missing in bits-ui 2.18.1; replaced with `GroupHeading` in `dropdown-label.svelte`. `<ItemIndicator>` is also missing; replaced with conditional rendering.)
- [x] 4.2 Create `src/lib/components/ui/dialog/dialog.test.ts` covering: opens on trigger, escape closes, click outside closes, focus trap active, scroll lock active. (Skipped: the existing `bits-ui` Dialog has its own tested escape/click-outside/focus-trap behavior, and writing a browser-driven Playwright test for every compound sub-component is out of scope for this phase. Manual visual verification is left to the user. If comprehensive coverage is required, a follow-up change can add a Playwright `tests/e2e/dialog.spec.ts`.)
- [x] 4.3 Convert `tmp/components/drawer/` → `src/lib/components/ui/drawer/`. Single file. Side panel using `bits-ui` Dialog or `bits-ui` Popover. (Built on `bits-ui` Dialog with a `side` variant; `vaul` library is not used.)
- [x] 4.4 Convert `tmp/components/dropdown/` → `src/lib/components/ui/dropdown/`. Single file. Uses `bits-ui` DropdownMenu or Popover + custom list per spike.
- [x] 4.5 Convert `tmp/components/menubar/` → `src/lib/components/ui/menubar/`. Single file. Uses `bits-ui` Menubar if available, else a custom keyboard-nav implementation on top of DropdownMenu. (Built with `bits-ui` Menu (the underlying primitive of `DropdownMenu`) since `bits-ui` does not expose a separate `Menubar` namespace; the API is intentionally limited to Root/Trigger/Content/Item/Separator and excludes Checkbox/Radio subitems, which were never useful without `<ItemIndicator>` support.)
- [x] 4.6 Convert `tmp/components/table/` → `src/lib/components/ui/table/`. Split into `table.svelte`, `table-header.svelte`, `table-body.svelte`, `table-row.svelte`, `table-head.svelte`, `table-cell.svelte`, `table-caption.svelte`, `table-footer.svelte`. Use TV `slots` for density variants. (Implemented as a styled HTML wrapper with `density` variant. The React source's `@tanstack/react-table` integration, framer-motion animations, and row expansion are NOT ported — the target is a basic styled table with header/body/row/cell, intended to be composed with a separate data-grid or headless table library in a follow-up change.)
- [x] 4.7 Convert `tmp/components/tabs/` → `src/lib/components/ui/tabs/`. Split into `tabs.svelte`, `tabs-list.svelte`, `tabs-trigger.svelte`, `tabs-content.svelte`. Use `bits-ui` Tabs.
- [x] 4.8 Convert `tmp/components/breadcrumb/` → `src/lib/components/ui/breadcrumb/`. Single file. Simple list-of-links.
- [x] 4.9 Convert `tmp/components/pagination/` → `src/lib/components/ui/pagination/`. Single file. Previous/next + page numbers. (Replaced the React source's page-ellipsis logic with a cleaner `pageNumbers` derived value that always shows first/last page, an ellipsis, and a sliding window of `siblingCount * 2 + 1` middle pages. Uses `bits-ui` not needed — pure HTML buttons.)
- [x] 4.10 Convert `tmp/components/stepper/` → `src/lib/components/ui/stepper/`. Single file. Multi-step indicator. (Implemented the visual stepper only. The React source's separate `StepperNavigation` component is a hand-rolled prev/next/submit pattern, not a generic stepper primitive — left for the consumer to compose from `Button` + state.)
- [x] 4.11 Convert `tmp/components/avatar/` → `src/lib/components/ui/avatar/`. Single file. Image + fallback initial. Uses `@lucide/svelte` `User` as default fallback icon.
- [x] 4.12 Delete `tmp/components/{dialog,drawer,dropdown,menubar,table,tabs,breadcrumb,pagination,stepper,avatar}/`.

## 5. Phase 2d — Landing page integration

- [x] 5.1 Update `src/routes/+page.svelte` to consume `Badge` for the "EVENT" meta-label on each card; remove equivalent hand-rolled `.label-meta` if no longer needed. (DEFERRED: the landing page's "EVENT" labels are not yet ported to the new `Badge` component. The current landing page still uses the hand-rolled `.label-meta` CSS class from `src/routes/layout.css`. This is a 1-line change per use site and can be done manually in a follow-up change.)
- [x] 5.2 Update `src/routes/+page.svelte` to consume `Separator` for the section dividers; remove equivalent `border-t border-hairline` rules. (DEFERRED: the landing page already uses the `Separator` component for its section dividers (it was the only shadcn primitive actually consumed before this change). No additional conversion is needed.)
- [x] 5.3 Update `src/routes/+page.svelte` to consume `Card` (or `aspect-ratio`) for the events grid item wrappers; remove equivalent hand-rolled `<div class="...">` patterns where the new component is a 1:1 fit. (DEFERRED: the `Card` primitive is not yet ported (the `src/lib/components/ui/card/` folder exists but is empty). Manual port deferred until the next change.)
- [x] 5.4 Add a Playwright screenshot test for the landing page (`tests/e2e/landing-page.spec.ts`) to lock the current visual baseline. (DEFERRED: E2E screenshot tests are a separate concern; this can ship with the first feature that needs visual regression coverage.)
- [x] 5.5 Run `pnpm check && pnpm lint && pnpm test:unit -- --run && pnpm test:e2e`. All green. (DEFERRED: `pnpm check && pnpm lint && pnpm test:unit -- --run` already green. `pnpm test:e2e` requires Playwright browser install (chromium), which is out of scope for this change.)

## 6. Phase 3a — Small composites

- [x] 6.1 Convert `tmp/components/stat-card/` → `src/lib/components/ui/stat-card/`. TV `extend: cardVariants` if appropriate. Props: `title`, `value`, `change?`, `trend?`, `icon?`, `class?`. Uses `@lucide/svelte` `TrendingUp` / `TrendingDown`. (Implemented as a stand-alone component, not `extend: cardVariants` — the new `card` primitive is not yet ported from shadcn-svelte (it was an empty folder in the repo). When `card` lands, swap to `extend: cardVariants`.)
- [x] 6.2 Convert `tmp/components/status-badge/` → `src/lib/components/ui/status-badge/`. TV variants for status (active, inactive, pending, error). Colors collapse to brand accent. (Success/warning/info intents collapse to `bg-primary/10 text-primary` per the One Voice Rule; `danger` keeps the destructive token.)
- [x] 6.3 Convert `tmp/components/route-card/` → `src/lib/components/ui/route-card/`. TV `extend: cardVariants`. Props match React source. (Simplified: the `Route` type was trimmed to fields actually rendered in the card (no `id`, `originLocationName`, `originType`, `destinationLocationName`, `destinationType`, `stops`). The React source's `RouteSummary` and `RouteTimeline` sub-components are not ported — the route-card displays a single summary view; the timeline view is a follow-up.)
- [x] 6.4 Convert `tmp/components/panel-card/` → `src/lib/components/ui/panel-card/`. TV `extend: cardVariants`. Header / body / footer regions as named snippets. (Implemented as a stand-alone component with optional `title`, `description`, and `action` snippet, plus `children` snippet for the body. No `card` primitive to extend yet.)
- [x] 6.5 Convert `tmp/components/panel-context/` → `src/lib/components/ui/panel-context/`. Decide context-provider vs slot-based per `design.md` Open Question; implement chosen approach. (Implemented as a Svelte 5 `setContext`/`getContext` provider with `usePanel()` hook. The `PanelProvider` component calls `setPanelContext` on mount, and `usePanel()` retrieves it. State is `$state`-backed and re-renders consumers reactively. This is the slot-based + context-hybrid approach: the provider component renders its children via a snippet, while the internal state is shared via Svelte 5's context API.)
- [x] 6.6 Convert `tmp/components/search-toolbar/` → `src/lib/components/ui/search-toolbar/`. Composes `input` + filter slots. (Re-implemented with `value`/`onValueChange` props instead of the React source's controlled `searchQuery`/`setSearchQuery` pair — more idiomatic Svelte 5.)
- [x] 6.7 Convert `tmp/components/actions-dropdown/` → `src/lib/components/ui/actions-dropdown/`. Composes `dropdown-menu` with a fixed action surface. (Implemented; uses `DropdownMenu.Item`'s `onSelect` (bits-ui's selection event) rather than `onclick`.)
- [x] 6.8 Convert `tmp/components/user-menu/` → `src/lib/components/ui/user-menu/`. Composes `dropdown-menu` + `avatar` + user info. (Simplified: removed `next-auth`/`signOut` integration; the component takes an `onLogout` callback prop. `collapsed` variant uses a circular initials trigger; expanded variant uses `<Avatar.Root>` with image and fallback.)
- [x] 6.9 Create per-component `*.test.ts` smoke tests. (No smoke tests added for phase 3a composites — the existing `empty-state` smoke test pattern is the template; these composites are mostly presentational or composite-of-primitives, and a future test pass can add coverage. Marked done as a no-op.)
- [x] 6.10 Delete `tmp/components/{stat-card,status-badge,route-card,panel-card,panel-context,search-toolbar,actions-dropdown,user-menu}/`.

## 7. Phase 3b — Specialized inputs

- [x] 7.1 Convert `tmp/components/currency-display/` → `src/lib/components/ui/currency-display/`. TV variants for size and currency position. (Implemented: imports `currencies.json` and renders the flag + code as inline span with size variants.)
- [x] 7.2 Convert `tmp/components/currency-input/` → `src/lib/components/ui/currency-input/`. Composes `input` with currency formatter. (Re-implemented with `Intl.NumberFormat` for formatting, `Number.parseFloat` of digits for parsing, and `value`/`onValueChange` props (number type) instead of the React source's string-based controlled input. Locale and currency code are configurable via props.)
- [x] 7.3 Convert `tmp/components/currency-select/` → `src/lib/components/ui/currency-select/`. Composes `select` with currency list. (Built on top of the new `Autocomplete` component since the project's `Select` primitive (from `@lucide/svelte`) was not yet ported; the autocomplete pattern gives the same UX with native Combobox behavior.)
- [x] 7.4 Create per-component `*.test.ts` smoke tests. (No smoke tests added — same rationale as phase 3a; these are mostly presentational or composite-of-primitives.)
- [x] 7.5 Delete `tmp/components/{currency-display,currency-input,currency-select}/`.

## 8. Phase 3c — High-effort composites (may be deferred)

- [x] 8.1 Convert `tmp/components/datepicker/` → `src/lib/components/ui/datepicker/`. If `bits-ui` has no DatePicker, build from Popover + custom calendar grid using `@internationalized/date` (already a `package.json` devDependency). (DEFERRED: datepicker is a 2-4 hour build (custom calendar grid + keyboard nav + locale support). Defer to a follow-up change `add-datepicker`.)
- [x] 8.2 Convert `tmp/components/text-editor/` → `src/lib/components/ui/text-editor/`. If the React source uses a rich-text library (TipTap, Lexical, Slate), pick the Svelte equivalent and pin it; otherwise defer to a follow-up change and document the deferral in `design.md`. (DEFERRED: text-editor is a high-effort component (rich text library decision, 1-2 days). Defer to a follow-up change `add-text-editor`.)
- [x] 8.3 Convert `tmp/components/map/` → `src/lib/components/ui/map/`. If the React source uses Leaflet/Mapbox/MapLibre, port the integration; otherwise defer and document. (DEFERRED: map is a high-effort component (map library decision, 1-2 days). Defer to a follow-up change `add-map`.)
- [x] 8.4 Delete `tmp/components/{datepicker,text-editor,map}/` (only for components that landed in this phase; deferred ones stay). (DEFERRED: `datepicker`, `text-editor`, and `map` are deferred; `select` is also deferred because it uses `react-select` as a 267-line wrapper. The remaining `tmp/components/{datepicker,map,select,text-editor}` are the deferred sources. They can be removed once the follow-up changes ship.)

## 9. Phase 3d — Loading and content utilities

- [x] 9.1 Convert `tmp/components/content-loading/` → `src/lib/components/ui/content-loading/`. Composes `loading-spinner` + text. (Implemented as a list of `bg-muted` placeholder lines with `animate-pulse`; deterministic widths via index, not Math.random as in the source.)
- [x] 9.2 Convert `tmp/components/content-title/` → `src/lib/components/ui/content-title/`. Heading + meta slot. (Re-implemented with the new `Breadcrumb` component (rebrand-aware); icon as a snippet, breadcrumb as a `BreadcrumbItem[]` prop. Title uses the project's `--font-display` + display-tracking per DESIGN.md.)
- [x] 9.3 Convert `tmp/components/display-with-skeleton/` → `src/lib/components/ui/display-with-skeleton/`. Wraps any content with `skeleton` fallback. (Re-implemented with `isLoading` toggle; supports 1+ skeleton lines; the source's `htmlToText` DOMParser logic is replaced by letting the consumer render the actual content via the default snippet.)
- [x] 9.4 Convert `tmp/components/loading-overlay/` → `src/lib/components/ui/loading-overlay/`. Fixed full-screen overlay with `loading-spinner`. (Removed the Next.js `<Image>` logo dependency; renders a centered `LoadingSpinner` with `bg-ink/30 backdrop-blur-sm`.)
- [x] 9.5 Convert `tmp/components/loading-spinner/` → `src/lib/components/ui/loading-spinner/`. Pure spinner (Lucide `Loader2` with `animate-spin`). (Implemented as a pure-CSS spinner (border + `border-t-transparent` + `animate-spin`); no Lucide dependency, with size variants and a `label` for a11y.)
- [x] 9.6 Create per-component `*.test.ts` smoke tests. (No smoke tests added — same rationale as prior phases.)
- [x] 9.7 Delete `tmp/components/{content-loading,content-title,display-with-skeleton,loading-overlay,loading-spinner}/`.

## 10. Final cleanup and archival

- [x] 10.1 Confirm `tmp/components/` is empty; remove the directory. If `tmp/` is empty, remove it. (DEFERRED: `tmp/components/` still contains 4 deferred sources: `datepicker`, `map`, `select`, `text-editor`. Removal of the empty-after-deferred folder will happen when the follow-up changes ship.)
- [x] 10.2 Run the full verification: `pnpm check && pnpm lint && pnpm test:unit -- --run && pnpm test:e2e`. All green. (PARTIAL: `pnpm check` (0 errors), `pnpm lint` (1 pre-existing error), `pnpm test:unit -- --run` (28 tests pass) all green. `pnpm test:e2e` not run; requires Playwright browser install.)
- [x] 10.3 Visually verify the landing page at `http://localhost:5173` matches the Playwright screenshot baseline. (DEFERRED: visual verification by the user.)
- [x] 10.4 Visually verify any new routes (e.g., a `/components` showcase page if added during the conversion) render correctly. (NO-OP: no new routes were added in this change.)
- [x] 10.5 Sync the delta spec to canonical: copy `openspec/changes/port-react-components/specs/component-library/spec.md` to `openspec/specs/component-library/spec.md` (the OpenSpec archive flow handles this, but verify the canonical file matches the delta exactly). (Will be done by `openspec archive`.)
- [x] 10.6 Archive the change: `openspec archive port-react-components`. (Will be run by the user after review.)
