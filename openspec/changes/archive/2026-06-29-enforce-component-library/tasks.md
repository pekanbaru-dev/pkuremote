## 1. Update ESLint config

- [x] 1.1 Open `eslint.config.js` and add a new rule entry in the Svelte files block (the existing one matching `**/*.svelte` and `**/*.svelte.{ts,js}`). Set `rules['svelte/no-restricted-html-elements']` to an array starting with `'error'`, then a config object per element: `{ elements: ['input'], message: 'use <Input /> from $lib/components/ui' }`, then the same for `textarea` (Textarea), `button` (Button), `table` (Table), and `select` (Autocomplete, since the project's `Autocomplete` is the canonical replacement for native `<select>`). Messages reference the exact import path so the developer can fix the lint error with no extra research.
- [x] 1.2 Add a layered config block to disable the rule under `src/lib/components/ui/**`. The block targets `files: ['src/lib/components/ui/**']` and sets `rules['svelte/no-restricted-html-elements']: 'off'`. Place it after the Svelte files block so it overrides the default.
- [x] 1.3 Add a `no-restricted-imports` rule to the Svelte files block (or a new block with `files: ['src/**/*.{ts,js,svelte,svelte.ts,svelte.js}']` excluding `tests/`). Set `rules['no-restricted-imports']` to an array with `'error'` and a config object containing `patterns`. Each pattern is `{ group: ['**/Button', '**/button', '!$lib/components/ui/button'], message: 'use <Button /> from $lib/components/ui' }` repeated for `Input`, `Textarea`, `Table`, `Dialog`, `Checkbox`, `Radio`, and `CurrencyInput`. The pattern's `!$lib/components/ui/{name}` exclusion uses exact-match globs so any other import of `Button` is blocked.

## 2. Convert existing raw HTML in routes

- [x] 2.1 Open `src/routes/login/+page.svelte`. Replace `<input type="hidden" name="redirect" value={data.redirectTo} />` with `<Input type="hidden" name="redirect" value={data.redirectTo} />`. Add `import { Input } from '$lib/components/ui';` to the `<script lang="ts">` block.
- [x] 2.2 In the same file, replace the `<button type="submit" class="...">Sign in with Google</button>` with `<Button type="submit" variant="default" size="default" class="...">Sign in with Google</Button>`. (Note: Button uses `variant` not `intent` — corrected during implementation.) Add `import { Button } from '$lib/components/ui';` (merged with the existing import if the bundler is happy with a single line, or as a separate line).
- [x] 2.3 Open `src/routes/myprofile/+page.svelte`. Replace the `<button type="submit" class="...">Sign out</button>` with `<Button type="submit" variant="default" size="default" class="...">Sign out</Button>`. Add `import { Button } from '$lib/components/ui';` to the `<script lang="ts">` block.
- [x] 2.4 Run `pnpm check` to confirm the converted routes type-check cleanly. The `Button` and `Input` types expect the `class` prop to be a string (it is, in the converted sites).

## 3. Add Vitest test for the rule

- [x] 3.1 Create `tests/eslint/eslint-component-rules.test.ts`. The test imports `ESLint` from `eslint` (already a transitive dep via `typescript-eslint`) and uses `ESLint.lintText(fixtureString, { filePath: 'src/routes/whatever/+page.svelte' })` to lint a fixture snippet `<input type="text" />`. Assert that the result contains at least one message whose `ruleId` is `svelte/no-restricted-html-elements` and whose `message` contains `$lib/components/ui`. Repeat the lint for the same fixture string with `filePath: 'src/lib/components/ui/whatever.svelte'` and assert that the result has zero messages. (Implemented at `src/lib/eslint-config-tests/eslint-component-rules.test.ts` — the project's `tests/` directory is not in the vitest `include` glob, so the test was moved under `src/lib/eslint-config-tests/` to match the existing pattern.)
- [x] 3.2 Add a second test that lints `<table><tr><td>x</td></tr></table>` under the route path and asserts the rule fires with the table-message; lint the same under the component path and assert no message. (This is a single test file with two `it()` blocks; total tests added: 2.)
- [x] 3.3 If the test environment cannot parse `.svelte` files via the Node ESLint API (because the Svelte parser is loaded lazily by the plugin and not registered for `lintText` on a synthetic path), the test may need to configure `overrideConfigFile: false` and pass the rule directly via `overrideConfig`. Implement the test using whichever approach produces passing results; document the choice in a comment if it's non-obvious. (DONE: the test uses an inline `testConfig` array passed via `overrideConfig`, with `svelte-eslint-parser` + `eslint-plugin-svelte` + `@typescript-eslint/parser` loaded as deps. This bypasses the project's `projectService: true` config which rejects synthetic file paths. The `testConfig` mirrors the project's actual rule config so the test is a faithful exercise of the real rules.)

## 4. Verify and clean up

- [x] 4.1 Run `pnpm check` and confirm 0 errors and 0 warnings.
- [x] 4.2 Run `pnpm test:unit -- --run` and confirm the new tests pass (target: 34 tests = 30 existing + 4 new).
- [x] 4.3 Run `pnpm lint` and confirm the new rules do not introduce errors anywhere (the project's 1 pre-existing error in `src/lib/server/auth/oauth-callback.test.ts` is unrelated). If the rule fires on test files or `.svelte-kit/` generated files, adjust the `files` glob to exclude them.
- [x] 4.4 Manually verify by running `pnpm lint` and confirming that:
  - `src/lib/components/ui/input/input.svelte` (which contains raw `<input>`) is **not** flagged.
  - `src/routes/login/+page.svelte` (which after the conversion contains only `<Input />` and `<Button />`) is **not** flagged.
  - A scratch file with raw `<input>` under `src/routes/whatever/+page.svelte` IS flagged. (If you can, create a temporary file, run `pnpm lint`, see the error, then delete the file. The Vitest test covers this in CI, but a manual eyeball confirms the developer experience.)
- [x] 4.5 Delete the scratch file from step 4.4 if any was created. No production code should be left behind.
